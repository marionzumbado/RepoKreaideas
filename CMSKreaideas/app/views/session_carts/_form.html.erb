<%= form_for(@session_cart) do |f| %>
  <% if @session_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@session_cart.errors.count, "error") %> prohibited this session_cart from being saved:</h2>

      <ul>
      <% @session_cart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
