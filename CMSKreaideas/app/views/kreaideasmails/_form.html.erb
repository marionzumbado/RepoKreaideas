<%= form_for(@kreaideasmail) do |f| %>
  <% if @kreaideasmail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kreaideasmail.errors.count, "error") %> prohibited this kreaideasmail from being saved:</h2>

      <ul>
      <% @kreaideasmail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_field :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
