<%= render 'refinery/admin/search_header', :url => refinery.albums_admin_albums_path %>
<div class='pagination_container'>
  <% if @albums.any? %>
    <%= render 'albums' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
