<% content_for :body do %>
  <ul id="albums">
    <% @albums.each do |album| %>
      <li>
        <%= link_to album.name, refinery.albums_album_path(album) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
