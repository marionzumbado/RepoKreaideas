<%= render 'refinery/admin/search_header', :url => refinery.subcategories_admin_subcategories_path %>
<div class='pagination_container'>
  <% if @subcategories.any? %>
    <%= render 'subcategories' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
