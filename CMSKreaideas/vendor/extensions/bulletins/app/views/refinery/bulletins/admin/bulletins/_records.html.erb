<%= render 'refinery/admin/search_header', :url => refinery.bulletins_admin_bulletins_path %>
<div class='pagination_container'>
  <% if @bulletins.any? %>
    <%= render 'bulletins' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
